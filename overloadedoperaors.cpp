//overloaded operators

/*
makes main easier to write and more consistent

with overlaoded operators

without									with
s.printData();							cout << s;
s.setData();								cin >> s;
sum.add(a,b);							sum = a + b;
										if(s1 > s2)
											-;
*/